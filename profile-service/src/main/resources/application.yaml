server:
  port: 8083
  servlet:
    context-path: /profile
spring:
  application:
    name: profile-service
  neo4j:
    uri: ${URI_NEO4J}
    authentication:
      username: ${NEO4J_USERNAME}
      password: ${NEO4J_PASSWORD}